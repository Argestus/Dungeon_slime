const DOMmaps = document.querySelector('.map')
console.log(DOMmaps);

let tile = 0;

for (let i = 0; i < maps.length; i++) {
    const mapRow = maps[i];
        for (let j = 0; j < maps[i].length; j++) {
            const tile = maps[i][j];
            return tile; 
        }
}
console.log(tile);

// function generateMap(maps) {
//     if (block === 1) {
        
//     } else {
        
//     }
// }